CREATE TABLE AUTHOR
(
  ID VARCHAR(36) PRIMARY KEY,
  NAME VARCHAR(50)
);

CREATE TABLE VIDEO
(
  ID VARCHAR(36) PRIMARY KEY,
  NAME VARCHAR(50),
  AUTHOR_ID VARCHAR(36),
  S3_KEY VARCHAR(36),
  CONSTRAINT VIDEO_AUTHOR_ID_fk FOREIGN KEY (AUTHOR_ID) REFERENCES AUTHOR (ID)
);